type Query {
  users: [User!]!
}

type Mutation {
  signup(name: String!, password: String!, avatar: String!): AuthPayload
  login(name: String!, password: String!): AuthPayload
}

type AuthPayload {
  user: User
  token: String
}

type User {
  id: ID!
  name: String!
  avatar: String!
}
